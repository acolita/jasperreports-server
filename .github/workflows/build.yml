name: JasperReports Docker Image CI/CD

on:
  push:
    branches:
      - main  
  pull_request:
    branches:
      - main  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          IMAGE_VERSION=$(date +%Y%m%d%H%M%S)-${{ github.sha }}
          docker build -t jasper_image:$IMAGE_VERSION .

      - name: Push Docker image to GitHub Packages
        run: |
          IMAGE_VERSION=$(date +%Y%m%d%H%M%S)-${{ github.sha }}
          docker tag jasper_image:$IMAGE_VERSION ghcr.io/acolita/jasper-dockerfile:$IMAGE_VERSION
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker push ghcr.io/acolita/jasper-dockerfile:$IMAGE_VERSION

      - name: Push Docker image with 'latest' tag
        run: |
          docker tag jasper_image:$IMAGE_VERSION ghcr.io/acolita/jasper-dockerfile:latest
          docker push ghcr.io/acolita/jasper-dockerfile:latest
